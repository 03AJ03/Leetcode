import pandas as pd

def find_valid_emails(users: pd.DataFrame) -> pd.DataFrame:
    pattern=r'^[a-zA-Z0-9_]+@[a-zA-z]+\.com$'
    return users[users['email'].str.match(pattern)].sort_values(by='user_id')
