import pandas as pd

def biggest_single_number(my_numbers: pd.DataFrame) -> pd.DataFrame:
    df=my_numbers[~my_numbers['num'].duplicated(keep=False)]
    return pd.DataFrame({'num': [df['num'].max()]})
