import pandas as pd

def article_views(views: pd.DataFrame) -> pd.DataFrame:
    df=views.copy()
    df=df[df['author_id']==df['viewer_id']]
    df=df['author_id'].drop_duplicates().rename('id')
    df=df.sort_values()
    return df.to_frame()
