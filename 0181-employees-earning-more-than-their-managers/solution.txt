import pandas as pd

def find_employees(employee: pd.DataFrame) -> pd.DataFrame:
    df=pd.merge(employee,employee,left_on='managerId',right_on='id',suffixes=('_emp','_manager'))
    df=df[df['salary_emp']>df['salary_manager']]
    return df[['name_emp']].rename(columns={'name_emp': 'Employee'})
